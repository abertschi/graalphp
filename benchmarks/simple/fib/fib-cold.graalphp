<?php

function fib($a) {
if ($a < 2) {
  return 1;
}
return fib($a - 1) + fib($a - 2);
}


$start = graalphp_time_ns();
$val = fib(30);
$stop = graalphp_time_ns();

$res = ($stop - $start);
println ($val);
println ($res);
println ($res / 1000.0);
println ($res / 1000.0 / 1000.0);

?>
